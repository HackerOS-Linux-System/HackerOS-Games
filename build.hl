// cargo
// love
// odin
// npm

> cd the-racer && cargo build --release && cd ..
> cd bit-jump && tar && sudo mv && cd ..
> cd starblaster && cargo build --release && cd ..
> cd bark-squadron && npm run dist && cd ..
> cd app && npm run build
  
